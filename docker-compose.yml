services:
  ocr-api:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./awsummit-f2ce2-5cbe291d4779.json:/app/credentials.json:ro
    environment:
      - GOOGLE_APPLICATION_CREDENTIALS=/app/credentials.json
